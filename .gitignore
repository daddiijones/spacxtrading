# dependencies
/node_modules

# build output
/dist

# env files (secrets — never commit these)
.env*

# misc
.DS_Store
*.pem

# debug
npm-debug.log*

# uploaded user content (runtime data, not source — app recreates this dir on startup)
/server/uploads

# editor
.vscode/*
!.vscode/extensions.json
